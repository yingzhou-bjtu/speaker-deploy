# 板端启动说明

部署目录（开发板）：`/speaker_test_zhouying/deploy/`  
PC 端源码：`01_机器人的声纹识别项目/deploy/`  
PC 端同步：项目根目录执行 `./sync_to_board.sh`（默认同步后重启服务）

---

## 1. 从 PC 同步到开发板

```bash
cd 01_机器人的声纹识别项目

RESTART=0 ./sync_to_board.sh   # 只同步脚本，不重启服务
./sync_to_board.sh             # 同步脚本并重启服务
```

---

## 2. robotmedia 前提（TCP 模式必做）

`/usr/share/robotmedia/config/config.ini`：

```ini
[vad_pcm_socket]
enable = 1
```

改后执行 `systemctl restart robotmedia`。  
确认监听：`ss -tlnp | grep 5001`

未开启时 `:5001` 无 PCM，`SV_PCM_SOURCE=tcp` 无法工作，请改用 §4 备选方案。

---

## 3. 板端启动（TCP 对接）

```bash
cd /speaker_test_zhouying/deploy
./clear_state.sh                    # 可选
SV_PCM_SOURCE=tcp ./start_robot.sh
```

- 连接 **127.0.0.1:5001**，收 robotmedia 推送的 PCM
- 默认 **`pcm-vad-end-audio=auto`**：TCP 帧数与 `silero_vad_*.wav` 对齐则内存 embed，否则读 wav
- 单进程同时跑 **HTTP API**（默认 `:8765`）与 **流式聚类**

| `SV_PCM_VAD_END_AUDIO` | 行为 |
| --- | --- |
| `auto`（默认） | 对齐走内存，不齐读 wav |
| `wav` | 始终读 wav，段界与落盘文件一致，处理延迟较高 |
| `tcp` | 始终用 TCP 缓冲，处理延迟较低，段界可能与 wav 不一致 |

**示例**：

```bash
SV_PCM_SOURCE=tcp SV_VERBOSE=1 ./start_robot.sh
```

---

## 4. 备选：仅落盘 wav

TCP 排障期间或 robotmedia 未开 socket 时：

```bash
./clear_state.sh
./start_robot.sh
# 等价 SV_PCM_SOURCE=wav_dir
```

---

## 5. 常用环境变量

| 变量 | 默认 | 含义 |
| --- | --- | --- |
| `SV_PCM_SOURCE` | `wav_dir` | tcp 连 :5001，wav_dir 扫目录，勿用 both |
| `SV_PCM_WAV_DIR` | `/userdata/cloud_voice` | robotmedia 写 `silero_vad_*.wav` 的目录 |
| `SV_PCM_MIN_DURATION_SEC` | `2.0` | 短于该时长的段忽略 |
| `SV_PCM_SEGMENT_END_IDLE_SEC` | `1.0` | TCP 段末静音多久后 analyze |
| `SV_API_PORT` | `8765` | HTTP API |
| `SV_VERBOSE` | 空 | 非空则 `--verbose` |
| `SV_ROBOT_LOG` | `/data/local/tmp/sv_robot.log` | 日志路径 |
| `SV_ORT_THREADS` | `4` | ONNX 线程 |
| `SV_MAX_HDBSCAN_UTTERANCES` | `60` | 流式缓冲上限 |

---

## 6. 检查是否在跑

```bash
pgrep -af robot_service.py
curl -s http://127.0.0.1:8765/v1/health
curl -s http://127.0.0.1:8765/v1/panel
tail -f /data/local/tmp/sv_robot.log
```

---

## 7. 停止

```bash
pkill -f robot_service.py
# 或
./clear_state.sh
```

---

## 8. 仅测 5001 是否有 PCM

先停声纹，避免两个客户端同时连 5001：

```bash
pkill -f robot_service.py
.venv/bin/python scripts/probe_tcp_5001.py --duration 60
```

对着麦克风发声后，日志应出现 `[pcm] session #N start` 及字节计数递增。
